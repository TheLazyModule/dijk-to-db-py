services:
  db:
    image: "postgis/postgis:15-3.4-alpine"
    environment:
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "london"
      POSTGRES_DB: "london_routes"
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

